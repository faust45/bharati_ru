function() {
  /*
  function listenFeed() {
    var feed = app.db.changes(null, {
          filter: 'four_up/partner_step', 
          include_docs: true,
        });
 
    feed.onChange(function(doc) {
      $.log('feed doc come', doc);
    });
  }
  */
}

