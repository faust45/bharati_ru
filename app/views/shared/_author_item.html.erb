<% html_class = "act" if is_selected_author?(author_item) %>
<div class="pivot">
  <%= link_to(author_main_photo(author_item) + "<span>#{author_item.display_name}</span>".html_safe, author_path(author_item), :class => html_class) %>
</div>
