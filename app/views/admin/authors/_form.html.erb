<%= simple_form_for [:admin, @author], :url => url, :html => {:multipart => true, :method => form_method} do |f| %>
  <%= f.input :full_name    %>
  <%= f.input :display_name %>
  <%= f.input :slug %>

  <%= f.input :main_photo, :as => :img_attachment %>
  <%= f.input :description, :as => :text, :input_html => { :class => 'mceEditor', :cols => 70} %>

  <%= f.submit 'Save' %>
<% end %>
