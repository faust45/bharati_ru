<div class="new">
  <% if author_item.cover %>
    <%= link_to photo_thumb(author_item.cover, {:width =>91, :height => 119}, true), show_publication_path(author_item) %></a>
  <% end %>
  <div class="name"><%= link_to "“#{author_item.title}”", show_publication_path(author_item) %></div>
  <p><%= author_item.description.truncate(100) %></p>
  <div class="date"><%= d author_item.created_at %></div>
</div>
